version: "2"  # musi być string, nie liczba

run:
  concurrency: 4
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - bodyclose
    - dogsled
    - dupl
    - funlen
    - goconst
    - gocyclo
    - gosec
    - lll
    - misspell
    - nakedret
    - revive
    - unconvert
    - unparam
    - whitespace

  settings:  
    funlen:
      lines: 120
      statements: 60
    gocyclo:
      min-complexity: 15
    lll:
      line-length: 120
    revive:
      severity: warning

issues:
  exclude-defaults: false  
  max-issues-per-linter: 0
  max-same-issues: 0
  exclusions:               
    rules:
      - linters:
          - revive
        text: "exported: exported"
      - linters:
          - revive
        text: "package-comments"
      - path: _test\.go
        linters:
          - funlen